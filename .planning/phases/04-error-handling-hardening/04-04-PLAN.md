---
phase: 04-error-handling-hardening
plan: 04
type: execute
wave: 2
depends_on:
  - "04-01"
files_modified:
  - src/pages/GroupPage.tsx
  - src/pages/LobbyPage.tsx
  - src/components/game/GameChat.tsx
autonomous: true
requirements:
  - ERR-01
  - ERR-02
  - ERR-03
must_haves:
  truths:
    - "GroupPage catch block uses catch (err: unknown) + getErrorMessage"
    - "GroupPage replaces alert() with setError() state"
    - "GroupPage .single() for createLobby checks error"
    - "LobbyPage .single() calls check error before accessing data"
    - "GameChat .single() call checks error before adding message to state"
  artifacts:
    - path: "src/pages/GroupPage.tsx"
      provides: "Group page with typed error handling, no alert()"
      contains: "getErrorMessage"
    - path: "src/pages/LobbyPage.tsx"
      provides: "Lobby page with .single() error checks"
      contains: "error"
    - path: "src/components/game/GameChat.tsx"
      provides: "Game chat with .single() error check"
  key_links:
    - from: "src/pages/GroupPage.tsx"
      to: "src/lib/errorHandler.ts"
      via: "import"
      pattern: "import.*errorHandler"
---

<objective>
Fix error handling in GroupPage, LobbyPage, and GameChat component.

Purpose: GroupPage has catch(err: any) + alert(). LobbyPage has unchecked .single() calls. GameChat has an unchecked .single() in realtime callback. These are related multiplayer-lobby files.
Output: All three files use typed errors with proper .single() checks.
</objective>

<execution_context>
@C:/DEV/LernAPP/LernApp/.claude/get-shit-done/workflows/execute-plan.md
@C:/DEV/LernAPP/LernApp/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@src/pages/GroupPage.tsx
@src/pages/LobbyPage.tsx
@src/components/game/GameChat.tsx
@src/lib/errorHandler.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix GroupPage.tsx error handling</name>
  <files>src/pages/GroupPage.tsx</files>
  <action>
Import `{ getErrorMessage }` from `@/lib/errorHandler`.

Add an `error` state: `const [error, setError] = useState('');`

Fix these issues:

1. **createLobby() line ~51-64** — `.select().single()` for new session:
   Currently does `const { data: session } = ...` followed by `if (!session) throw new Error(...)`.
   Add error destructuring: `const { data: session, error: createError }`.
   Change check to: `if (createError || !session) throw new Error(getErrorMessage(createError) || 'Fehler beim Erstellen');`

2. **createLobby() catch block line ~81** — `catch (err: any) { alert(err.message); }`:
   Change to `catch (err: unknown) { setError(getErrorMessage(err)); }`.

3. **Add error display UI** — Inside the component return, after the "Lobby beitreten" Card section and before the lobby list, add:
   ```
   {error && (
     <div className="bg-red-500/10 border border-red-500/30 rounded-xl px-4 py-3 text-sm text-red-400 mb-6">
       {error}
     </div>
   )}
   ```

4. **Clear error on action** — Add `setError('')` at the start of `createLobby()`.
  </action>
  <verify>
    <automated>cd C:/DEV/LernAPP/LernApp && npx tsc --noEmit src/pages/GroupPage.tsx 2>&1 | head -20</automated>
  </verify>
  <done>
    - Zero `catch (err: any)` in GroupPage.tsx
    - Zero `alert()` calls
    - .single() for createLobby checks error
    - Error displayed in UI via error state
  </done>
</task>

<task type="auto">
  <name>Task 2: Fix LobbyPage.tsx and GameChat.tsx .single() calls</name>
  <files>src/pages/LobbyPage.tsx, src/components/game/GameChat.tsx</files>
  <action>
**LobbyPage.tsx:**

Fix these .single() calls:

1. **fetchData() line ~29** — `const { data: s } = await supabase.from('game_sessions').select('*').eq('id', sessionId).single();`
   Add error: `const { data: s, error: sessionError }`.
   If `sessionError`, log: `console.error('Failed to load lobby session:', sessionError.message);` and return.

2. **fetchData() line ~33** — `const { data: p } = await supabase.from('game_players').select(...)...eq('session_id', sessionId);`
   This is NOT a .single() call (returns array), so no change needed.

3. **joinLobby() line ~50-55** — `const { data: existing } = await supabase.from('game_players').select('id').eq(...).single();`
   Add error: `const { data: existing, error: existingError }`.
   PGRST116 (not found) is expected here (means player hasn't joined yet). Change to: `if (!existing && (!existingError || existingError.code === 'PGRST116'))` for the insert block. If unexpected error, log and return.

No `catch (err: any)` patterns exist in LobbyPage — no changes needed there.

**GameChat.tsx:**

Fix this .single() call:

1. **Realtime callback line ~41-46** — Inside the `on('postgres_changes', ...)` callback:
   ```
   const { data } = await supabase.from('chat_messages').select('*, profile:profiles(username, is_online)').eq('id', payload.new.id).single();
   if (data) setMessages(prev => [...prev, data]);
   ```
   Add error: `const { data, error }`. If `error`, log `console.error('Failed to load chat message:', error.message)` and return (don't add to state). The existing `if (data)` check already prevents null access, but explicit error logging helps debug issues.

No `catch (err: any)` patterns exist in GameChat — no changes needed there.
  </action>
  <verify>
    <automated>cd C:/DEV/LernAPP/LernApp && npx tsc --noEmit src/pages/LobbyPage.tsx src/components/game/GameChat.tsx 2>&1 | head -20</automated>
  </verify>
  <done>
    - All .single() calls in LobbyPage check error before accessing data
    - PGRST116 handled gracefully in joinLobby
    - GameChat .single() in realtime callback checks error
    - Both files compile without errors
  </done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes for all three files
- `grep -n "err: any\|alert(" src/pages/GroupPage.tsx src/pages/LobbyPage.tsx src/components/game/GameChat.tsx` returns no results
- All .single() calls have error destructuring
</verification>

<success_criteria>
- Zero `catch (err: any)` in all three files
- Zero `alert()` in GroupPage
- All .single() calls check error
- Error messages displayed in UI where applicable
</success_criteria>

<output>
After completion, create `.planning/phases/04-error-handling-hardening/04-04-SUMMARY.md`
</output>
