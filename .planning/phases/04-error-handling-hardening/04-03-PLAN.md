---
phase: 04-error-handling-hardening
plan: 03
type: execute
wave: 2
depends_on:
  - "04-01"
files_modified:
  - src/pages/ChallengePage.tsx
  - src/pages/TournamentPage.tsx
autonomous: true
requirements:
  - ERR-01
  - ERR-02
  - ERR-03
must_haves:
  truths:
    - "ChallengePage catch blocks use catch (err: unknown) + getErrorMessage(err)"
    - "ChallengePage .single() at createChallenge checks error before using target data"
    - "TournamentPage catch block uses catch (err: unknown) + getErrorMessage(err)"
    - "TournamentPage .single() at createTournament checks error before using tournament data"
    - "TournamentPage replaces alert() with setError() state pattern"
    - "User sees error messages in UI instead of browser alert dialogs"
  artifacts:
    - path: "src/pages/ChallengePage.tsx"
      provides: "Challenge page with typed error handling"
      contains: "getErrorMessage"
    - path: "src/pages/TournamentPage.tsx"
      provides: "Tournament page with typed error handling, no alert()"
      contains: "getErrorMessage"
  key_links:
    - from: "src/pages/ChallengePage.tsx"
      to: "src/lib/errorHandler.ts"
      via: "import"
      pattern: "import.*errorHandler"
    - from: "src/pages/TournamentPage.tsx"
      to: "src/lib/errorHandler.ts"
      via: "import"
      pattern: "import.*errorHandler"
---

<objective>
Fix error handling in ChallengePage and TournamentPage.

Purpose: These are the most complex pages with multiple catch blocks, .single() calls, and alert() usage. TournamentPage is the worst offender with 6+ alert() calls.
Output: Both pages use typed errors and display errors in-UI instead of alert().
</objective>

<execution_context>
@C:/DEV/LernAPP/LernApp/.claude/get-shit-done/workflows/execute-plan.md
@C:/DEV/LernAPP/LernApp/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@src/pages/ChallengePage.tsx
@src/pages/TournamentPage.tsx
@src/lib/errorHandler.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix ChallengePage.tsx error handling</name>
  <files>src/pages/ChallengePage.tsx</files>
  <action>
Import `{ getErrorMessage }` from `@/lib/errorHandler`.

Fix these issues:

1. **createChallenge() line ~83** — `.single()` call for target profile lookup:
   `const { data: target } = await supabase.from('profiles').select('id').eq('username', targetUsername.trim()).single();`
   Add error destructuring: `const { data: target, error: targetError }`. If `targetError` or `!target`, throw `new Error('Spieler "${targetUsername}" nicht gefunden')`. This makes the check explicit.

2. **createChallenge() catch block line ~119** — `catch (err: any)`:
   Change to `catch (err: unknown)`. Replace `err.message` with `getErrorMessage(err)`:
   `setError(getErrorMessage(err));`

3. **respondToChallenge() catch block line ~168** — `catch (err: any)`:
   Change to `catch (err: unknown)`. Replace `err.message || 'Fehler...'` with `getErrorMessage(err)`:
   `setError(getErrorMessage(err));`

No other .single() calls in ChallengePage need fixing (the RPC calls already check error properly).
  </action>
  <verify>
    <automated>cd C:/DEV/LernAPP/LernApp && npx tsc --noEmit src/pages/ChallengePage.tsx 2>&1 | head -20</automated>
  </verify>
  <done>
    - Zero `catch (err: any)` in ChallengePage.tsx
    - .single() call for target profile checks error
    - getErrorMessage used in all catch blocks
    - TypeScript compiles without errors
  </done>
</task>

<task type="auto">
  <name>Task 2: Fix TournamentPage.tsx error handling + replace alert()</name>
  <files>src/pages/TournamentPage.tsx</files>
  <action>
Import `{ getErrorMessage }` from `@/lib/errorHandler`.

Add an `error` state to the component (it currently lacks one):
`const [error, setError] = useState('');`

Fix these issues:

1. **createTournament() line ~55-66** — `.select().single()` for new tournament:
   Add error destructuring: `const { data: t, error: createError }`.
   Replace `if (t)` with: `if (createError || !t) { setError(getErrorMessage(createError)); return; }`.
   Then proceed with the participant insert.

2. **createTournament() catch block line ~80** — `catch (err: any) { alert(err.message); }`:
   Change to `catch (err: unknown) { setError(getErrorMessage(err)); }`.

3. **joinTournament() line ~97** — `else alert('Bereits angemeldet')`:
   Change to `else setError('Bereits angemeldet');`.

4. **startTournament() lines ~103, ~118-126** — multiple `alert()` calls:
   - Line ~103 `alert('Mindestens 2 Spieler benoetigt')` -> `setError('Mindestens 2 Spieler benoetigt'); return;`
   - Line ~118-126 all `alert(...)` calls within the error handling block -> `setError(...)` calls using the same messages. The existing hint-based error discrimination is good, just replace `alert()` with `setError()`.

5. **Add error display UI** — After the main heading `<p className="text-slate-400 mt-1">...` add an error display block (same pattern as ChallengePage):
   ```
   {error && (
     <div className="bg-red-500/10 border border-red-500/30 rounded-xl px-4 py-3 text-sm text-red-400 mb-4">
       {error}
     </div>
   )}
   ```
   Place it right before the tournament list (after the header section, before `<div className="flex flex-col gap-4">`).

6. **Clear error on new actions** — Add `setError('')` at the start of `createTournament()`, `joinTournament()`, and `startTournament()`.
  </action>
  <verify>
    <automated>cd C:/DEV/LernAPP/LernApp && npx tsc --noEmit src/pages/TournamentPage.tsx 2>&1 | head -20</automated>
  </verify>
  <done>
    - Zero `catch (err: any)` in TournamentPage.tsx
    - Zero `alert()` calls in TournamentPage.tsx
    - .single() for createTournament checks error
    - Error messages displayed in UI via error state
    - TypeScript compiles without errors
  </done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes for both files
- `grep -n "err: any\|alert(" src/pages/ChallengePage.tsx src/pages/TournamentPage.tsx` returns no results
- `grep -n "getErrorMessage" src/pages/ChallengePage.tsx src/pages/TournamentPage.tsx` shows usage in both files
</verification>

<success_criteria>
- Zero `catch (err: any)` in both files
- Zero `alert()` calls in TournamentPage
- All .single() calls check error before data access
- Error messages appear in UI, not browser dialogs
</success_criteria>

<output>
After completion, create `.planning/phases/04-error-handling-hardening/04-03-SUMMARY.md`
</output>
