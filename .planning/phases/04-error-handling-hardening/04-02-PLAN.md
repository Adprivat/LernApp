---
phase: 04-error-handling-hardening
plan: 02
type: execute
wave: 2
depends_on:
  - "04-01"
files_modified:
  - src/stores/authStore.ts
  - src/stores/gameStore.ts
autonomous: true
requirements:
  - ERR-01
  - ERR-02
must_haves:
  truths:
    - "authStore.register() checks .single() error before using data"
    - "authStore.fetchProfile() checks .single() error before using profile data"
    - "authStore.updateProfile() checks .single() error before using data"
    - "gameStore.createSoloSession() checks .single() error before using session"
    - "gameStore.joinSession() handles .single() returning no data gracefully"
    - "gameStore.loadSession() checks .single() error before using session"
    - "No catch (err: any) remains in either store file"
  artifacts:
    - path: "src/stores/authStore.ts"
      provides: "Auth store with typed error handling"
      contains: "getErrorMessage"
    - path: "src/stores/gameStore.ts"
      provides: "Game store with typed error handling and .single() checks"
      contains: "getErrorMessage"
  key_links:
    - from: "src/stores/authStore.ts"
      to: "src/lib/errorHandler.ts"
      via: "import"
      pattern: "import.*errorHandler"
    - from: "src/stores/gameStore.ts"
      to: "src/lib/errorHandler.ts"
      via: "import"
      pattern: "import.*errorHandler"
---

<objective>
Fix error handling in both Zustand stores (authStore + gameStore).

Purpose: Stores are the data layer — every page depends on them. Fixing stores first means pages inherit safer data access patterns.
Output: Both stores use typed error handling with proper .single() checks.
</objective>

<execution_context>
@C:/DEV/LernAPP/LernApp/.claude/get-shit-done/workflows/execute-plan.md
@C:/DEV/LernAPP/LernApp/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@src/stores/authStore.ts
@src/stores/gameStore.ts
@src/lib/errorHandler.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix authStore.ts error handling</name>
  <files>src/stores/authStore.ts</files>
  <action>
Import `{ getErrorMessage }` from `@/lib/errorHandler`.

Fix the following .single() calls that lack error checking:

1. **register() line ~64** — `const { data: existing } = await supabase.from('profiles').select('id').eq('username', username).single();`
   This is intentionally checking if a profile exists. The current logic is: if `existing` truthy, throw "already taken". This is actually fine logically — a missing profile returns null data with an error, which means username is available. No change needed here since the logic works correctly (not-found = available).

2. **fetchProfile() line ~109-113** — `const { data: profile } = await supabase.from('profiles').select('*').eq('id', user.id).single();`
   Add error destructuring: `const { data: profile, error }`. If `error` and error is NOT a "not found" error (PGRST116), log it with `console.error('fetchProfile error:', error.message)`. The existing null check on `profile` already handles the data-missing case.

3. **updateProfile() line ~128-133** — `const { data } = await supabase.from('profiles').update(updates).eq('id', user.id).select().single();`
   Add error destructuring: `const { data, error }`. If `error`, log with `console.error('updateProfile error:', error.message)` and return early.

No `catch (err: any)` patterns exist in authStore currently (login/register already use proper try/catch with throw), so no catch blocks need changing.
  </action>
  <verify>
    <automated>cd C:/DEV/LernAPP/LernApp && npx tsc --noEmit src/stores/authStore.ts 2>&1 | head -20</automated>
  </verify>
  <done>
    - All .single() calls in authStore check for error before accessing data
    - No `catch (err: any)` in the file
    - getErrorMessage imported from errorHandler
    - TypeScript compiles without errors
  </done>
</task>

<task type="auto">
  <name>Task 2: Fix gameStore.ts error handling</name>
  <files>src/stores/gameStore.ts</files>
  <action>
Import `{ getErrorMessage }` from `@/lib/errorHandler`.

Fix the following issues:

1. **createSoloSession() line ~67-68** — `.select().single()` already has `if (error) throw error` on the next line. This is correct. No change needed.

2. **createSoloSession() catch block line ~96** — `catch (err)` — this already does NOT use `: any` and just re-throws. Correct as-is.

3. **joinSession() line ~106-111** — `const existing = await supabase.from('game_players').select('id').eq(...).single();`
   Currently checks `if (!existing.data)` to decide whether to insert. Add proper error handling: destructure as `const { data: existingData, error: existingError }`. The PGRST116 (not found) error means player doesn't exist yet — that's the "insert" path. Only log unexpected errors (non-PGRST116): `if (existingError && existingError.code !== 'PGRST116') console.error(...)`. Use `if (!existingData)` for the insert path.

4. **loadSession() line ~129-133** — `const { data: session } = await supabase.from('game_sessions').select('*').eq('id', sessionId).single();`
   Add error destructuring: `const { data: session, error }`. If `error`, log `console.error('loadSession error:', error.message)` and return. The existing `if (!session) return` already handles null data.

No other `catch (err: any)` patterns exist in gameStore.
  </action>
  <verify>
    <automated>cd C:/DEV/LernAPP/LernApp && npx tsc --noEmit src/stores/gameStore.ts 2>&1 | head -20</automated>
  </verify>
  <done>
    - All .single() calls in gameStore check for error
    - PGRST116 (not found) errors handled gracefully where expected
    - No `catch (err: any)` in the file
    - TypeScript compiles without errors
  </done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes for both store files
- `grep -n "err: any" src/stores/authStore.ts src/stores/gameStore.ts` returns no results
- `grep -n "getErrorMessage\|errorHandler" src/stores/authStore.ts src/stores/gameStore.ts` shows imports present
</verification>

<success_criteria>
- Zero `catch (err: any)` in either store
- All .single() calls have error checking
- Both files compile cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/04-error-handling-hardening/04-02-SUMMARY.md`
</output>
